node-js app that retrieve & serve realtime 1forge rates through websockets

# install

```shell
yarn install
```

# run the server

```shell
npx ts-node src/replicator.ts
```

# test the server

Using insomnia, postman or the client.html example.